# ─────────────────────────────────────────────────────────────────────
#  Store Intelligence — Python Dependencies
#  
#  Installation order matters:
#  1. Install torch FIRST (CPU-only in Docker):
#     pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cpu
#  2. Then install this file:
#     pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────

# ── ReID model (OSNet via torchreid) ────────────────────────────────
# NOTE: torchreid is installed from GitHub in the Dockerfile.
# If it fails, the API starts in "no-ReID" mode — events are still
# ingested and all intelligence endpoints work; only live camera
# processing is degraded. See reid/embedder.py for the fallback.
# torchreid @ git+https://github.com/KaiyangZhou/deep-person-reid.git

# ── Detection & Tracking ─────────────────────────────────────────────
ultralytics>=8.2.0              # YOLOv8 detection
supervision>=0.21.0             # ByteTrack + annotators

# ── Computer vision ──────────────────────────────────────────────────
# Use headless variant in Docker (no X11 / Qt required)
opencv-python-headless>=4.9.0
Pillow>=10.0.0

# ── Numerical / scientific ───────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                   # cosine distance fallback

# ── API layer ────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ── Observability ────────────────────────────────────────────────────
structlog>=24.1.0               # structured JSON logging
prometheus-client>=0.20.0       # /prometheus endpoint

# ── Async / HTTP ─────────────────────────────────────────────────────
anyio>=4.3.0
httpx>=0.27.0                   # used by tests

# ── Testing ──────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ── Utilities ────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # .env file support
click>=8.1.0                    # CLI entry-point flags
rich>=13.7.0                    # pretty terminal output
